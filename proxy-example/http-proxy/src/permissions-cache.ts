// Represents some sort of Permissions system.
// In this example code, everyone is authorized.
export class PermissionsCache {
  public async getPermissions(accessToken?: string, iTwinId?: string) {
    // Here, the permissions cache should be queried
    return true;
  }
}
